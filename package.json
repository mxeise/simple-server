{
  "name": "simple-server",
  "version": "1.0.0",
  "description": "Simple Server to test the network",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maximilian Eise",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  }
}
